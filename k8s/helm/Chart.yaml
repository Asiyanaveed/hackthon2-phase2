apiVersion: v2
name: todo-chatbot
description: A full-stack Todo Chatbot application with AI capabilities
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: postgresql
    version: "15.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
    alias: postgresql
